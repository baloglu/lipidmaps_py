# LIPID MAPS Python API

Welcome — this site documents the `lipidmaps` Python package.

Quick start:

```python
from lipidmaps import process_csv
ds = process_csv("data/myfile.csv", validate_data=True)